export const selectAddresses = (
  state
) => state.address.addresses;

export const selectAddressLoading = (
  state
) => state.address.loading;

export const selectSelectedAddress = (
  state
) => state.address.selectedAddress;